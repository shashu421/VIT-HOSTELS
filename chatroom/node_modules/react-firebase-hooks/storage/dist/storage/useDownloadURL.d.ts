import { storage, FirebaseError } from 'firebase';
import { LoadingHook } from '../util';
export declare type DownloadURLHook = LoadingHook<string, FirebaseError>;
declare const _default: (storageRef?: storage.Reference | null | undefined) => [string | undefined, boolean, FirebaseError | undefined];
export default _default;
