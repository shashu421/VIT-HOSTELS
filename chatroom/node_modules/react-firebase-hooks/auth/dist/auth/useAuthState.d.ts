import { auth, User } from 'firebase';
import { LoadingHook } from '../util';
export declare type AuthStateHook = LoadingHook<User, auth.Error>;
declare const _default: (auth: auth.Auth) => [User | undefined, boolean, auth.Error | undefined];
export default _default;
